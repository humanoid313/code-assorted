#include <limits.h>
#include <stdio.h>
#include <iostream>
using namespace std;
int main() { 
/*
	int c = 8; 
	int res = (++c) *(--c);
	printf("%d\n", c);
	c = 8;
	int res2 = (--c)*(++c);
	printf("%d\t%d\n", res, res2);
	
	double *p = NULL;*/
	
// 	To verify that 'string' and 'char' are promoted to bool	
/*
//	string s = "";
	char c;
//	bool b = s;	// String to bool - doesn't promote
	bool b;
	cout<<c<<endl;
	cout<<b<<endl;
	b = c;		// False as expected
	cout<<b<<endl;
	*/
// 	To check if reference to an array is possible
/*	int arr[3] = {0};
	int &refarr = arr[0];	//OK
//	int &refarr = arr;		//Illegal: invalid initialization of non-const reference of type ‘int&’ from a temporary of type ‘int*’
//	int &refarr[] = {arr[0], arr[1]}; // Illegal: declaration of ‘refarr’ as array of references
	cout<<refarr<<endl; */
	
}

